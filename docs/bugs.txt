singleToStr:
I did:
  x=.111
  x+=x  ;Displays correctly
  x+=x  ;Displays correctly
  x+=x  ;Displays correctly
  x+=x  ;Displays correctly
  x+=x  ;Displays correctly
  x+=x  ;Displays correctly
  x+=x  ;Displays as 41.208 instead of 14.208 !

sqrtSingle isn't decrementing the top 16 bits when it is supposed to.
