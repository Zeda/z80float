;sqrt(x^2-1)/BG(x,1)
#define var_x  fpOP1+152
xacosh:
  push hl
  push de
  push bc
  push af
  push bc
  call +_
  ex de,hl
  pop de
  call mov10
  pop af
  pop bc
  pop de
  pop hl
  ret
_:
  ;<insert code here>
  ret
